# Python dependencies for the PocketCFO API service.
#
# The SDK is installed from a prebuilt wheel vendored under frontend/vendor/.
# Vercel sandboxes each service to its own directory tree, so a path dep like
# "../sdk" (outside frontend/) is NOT visible at build time. Building the SDK to
# a wheel and installing it from inside frontend/ is the reliable way to "install
# the SDK into the frontend" on Vercel. Rebuild with:  cd sdk && uv build --wheel
# then copy dist/*.whl into frontend/vendor/.
#
# Path is relative to the service entrypoint dir (frontend/api/), so ../vendor
# points at frontend/vendor/.
../vendor/pocketcfo-0.1.0-py3-none-any.whl
fastapi[standard]>=0.128.1
python-multipart>=0.0.9
